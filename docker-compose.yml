services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: namesolver
      POSTGRES_USER: namesolver
      POSTGRES_PASSWORD: namesolver
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
